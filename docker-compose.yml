services:
  jekyll:
    build:
      context: .
      dockerfile: Dockerfile
    command: bundle exec jekyll serve --host 0.0.0.0 --watch --incremental
    ports:
      - 4000:4000
    volumes:
      - .:/srv/jekyll
